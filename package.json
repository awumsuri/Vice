{
  "name": "server-test",
  "version": "0.0.1",
  "scripts": {
    "build": "babel src -d dist",
    "start": "npm run build && node ~/Vice/dist/index.js"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0"
  }
}
